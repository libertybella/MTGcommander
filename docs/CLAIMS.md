# Cluster claims board

The single source of truth for **who is working on what**, so no work is
duplicated and no branches collide. Lives on the integration branch
(`comprehensive-plan`); git serializes claims.

## The claim protocol

1. **Before writing any code**, pull the latest integration branch and
   check this table. Never start a cluster that is `claimed` or
   `in-progress` by someone else.
2. **To claim**: edit *only your row* (status, owner, branch, date), commit
   just this file with the message `Claim: <cluster> (<owner>)`, and push
   to the integration branch immediately. If the push is rejected, pull —
   someone may have claimed first; first commit wins, re-pick if you lost.
3. **One in-progress cluster per owner.** Finish (or release) before
   claiming the next.
4. **To finish**: your wave-merge commit also flips the row to `done` with
   the flip count and new compile rate in Notes.
5. **To release**: set the row back to `open` (say why in Notes). A claim
   with no commits on its branch for **7 days** may be released by anyone.
6. Agents identify as `claude/<session-or-task>`, humans by name. The
   Owner column is who to ping, so make it reachable (GitHub handle once
   the repo flow is live).
7. Rows are append-friendly: add newly discovered clusters under their
   stream with status `open`. Don't delete done rows — they are the
   history.

Full rules of engagement: [../AGENTS.md](../AGENTS.md). Machinery detail
per cluster: [WORKSTREAMS.md](WORKSTREAMS.md).

## Status board

Statuses: `open` · `claimed` · `in-progress` · `done` · `blocked`

| Cluster | Stream | Status | Owner | Branch | Updated | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| Land-tap echo + Elenda + Grisly Salvage (Mirari's Wake, Vorinclex, Elenda ×2, Grisly Salvage) | E/F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-21 | Wave 123 merged: 5 flips, 894/2,009 (44.5%), 836 tests |
| Convoke + improvise | A | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 194 merged, together with delve: all three reduce a printed cost by spending a different resource, so they share `costRelief`. Convoke covers COLOURED pips first (a creature pays one generic OR one mana of its own colour, so spending a matching creature on a generic pip can strand a coloured one); improvise and delve cover generic only. The cost is reduced only as far as needed to make it payable, so nothing is tapped or exiled that the caster did not need — a documented auto-policy standing in for a choice the cast action has no field for. Legal-action enumeration works from POTENTIAL mana, so it uses a separate ceiling-based `reliefAdjustedCost` — a spell must never be offered that the payment path would refuse, nor hidden when it could be paid. Granted forms (Inspiring Statuary, Dazzling Theater) read the same helper. 6 flips (Chord of Calling, Treasure Cruise, City on Fire, Whir of Invention, Inspiring Statuary, Dazzling Theater) — 1,263/2,009 (62.9%), 1,075 tests |
| Dynamic-count vocabulary (one table, one validator) | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 206 merged: `parseGrantPredicate` reads the shared `parseDynamicCount` instead of the four-row `PER_COUNTS`, which retired the hand-written Nettlecyst branch, and `selfDiscount` gained both `total_creature_power` (Ghalta sums where the Henge takes the maximum) and a `perDynamicCount` so Embercleave's "{1} less for each attacking creature" is the same table again. Two silent bugs came out with it. The serializer's `per` allow-list had drifted to 7 of the union's 13, so a state holding Kor Spiritdancer's `auras_attached_to_it` failed to parse; it is now a total `Record<DynamicCount, true>`, and the count evaluator's trailing ternary chain is now a record keyed by the narrowed union, so a new member is a tsc error rather than a silent fall-through onto "artifact". And `dynamicCountOf` was called with no source at all, so every "attached to it" count was 0 — the buff compiled, typechecked, and did nothing. The source is not the right referent either: "it" is the object the ability AFFECTS, so an Equipment counts the EQUIPPED creature's attachments. 5 flips (Thran Power Suit, Strength of the Harvest, Sage's Reverie, Embercleave, Ghalta) — 1,311/2,009 (65.3%), 1,126 tests |
| Goad and attack requirements | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 207 merged: goad (CR 701.38) as a real mechanic rather than a compiled no-op. `CardInstance.goadedBy` is a LIST of goaders, because two opponents goading the same creature means it may attack neither, and each entry expires on its own goader's turn ("until your next turn"). `declareAttackers` already enforced Toski's "attacks each combat if able", so the must-attack half was a condition widened, not new machinery; the new half is the defender restriction, checked per declaration rather than in `assertLegalAttacker` because "attack a player other than you IF ABLE" has to know whether another defender existed at all. Shiny Impetus needed a static form (`{ kind: "goaded" }` in layer 6, goader = the source's controller), so the query merges instance and computed. Bident of Thassa is deliberately NOT goad — "attack this turn if able" leaves the defender free, which is a separate `mustAttackThisTurn` flag. Grenzo left as a miss on purpose: "target creature THAT PLAYER controls" needs a target requirement scoped to the trigger's subject player, and widening it to "not_own" would let it goad a third player's creature. 3 flips (Disrupt Decorum, Shiny Impetus, Bident of Thassa) plus Kardur's longhand half — 1,314/2,009 (65.4%), 1,133 tests |
| Keyword abilities that lower onto existing machinery | A | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 208 merged, the first of the keyword vein: roughly thirty one-away cards are one-away for the same reason, a bare keyword line the compiler does not read. This wave took the four whose reminder text is already expressible. Outlast is a tap ability at sorcery timing. Typecycling is plain cycling with a search in place of the draw, and "Basic landcycling" is the supertype form of that clause rather than a subtype. Vanishing needed one new effect, `remove_counter` with `sacrificeWhenEmpty`, because "when the last counter is removed" has no engine event to watch. Its negative case found a bug on the way in: keying the sacrifice on "none left" rather than "one was actually taken off" would kill any permanent that outlived its counters. 4 flips (Abzan Falconer, Lórien Revealed, Ash Barrens, Dreamtide Whale) — 1,318/2,009 (65.6%), 1,137 tests |
| Graveyard returns and the counter they arrive with | A/F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 209 merged, continuing the keyword vein: `move_card.withCounter` puts the counter on as part of the ARRIVAL, so the state-based sweep that follows sees the shrunken creature rather than a full-size one. Persist the card and Persist the keyword are the same effect, and the keyword adds an intervening-if (`self_no_counter`) which is the only thing stopping it from looping — a creature that came back already carries the counter. That condition is answerable at all only because counters survive a zone change in this engine, where CR 400.7 would make the graveyard card a new object with none; noted as a documented divergence that happens to give the right answer here. Unearth is a pure lowering — a graveyard activation at sorcery speed whose riders (`gainsHaste`, `atEndStep: "exile"`) already existed; the "or if it would leave the battlefield" half is a documented approximation. The negative case found the wave-199 shape again: the whole graveyard target family checked mana value and nothing else, so "nonlegendary" parsed and was ignored — `violatesCharacteristicFilter` now runs there too, which brings colours and subtypes with it. 3 flips (Persist, Glen Elendra Archmage, Molten Gatekeeper) — 1,321/2,009 (65.8%), 1,141 tests |
| Eternalize (a copy made from the graveyard) | A | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 210 merged: `copy_token` gained `setColors`/`addSubtypes` beside the `setPt` Offspring already used, and will now copy a card in the GRAVEYARD, which it used to refuse outright. The overrides go on a CLONED definition — writing them onto the shared one would quietly turn every other copy of the card into a black Zombie. 2 flips (Timeless Witness, Fanatic of Rhonas) — 1,323/2,009 (65.9%), 1,143 tests. Two neighbours were probed and left: Blade of Selves GRANTS myriad to the equipped creature, which needs the granted-trigger statics Kaldra is also waiting on, and Encore needs a copy that must attack one NAMED player — the wave-207 requirements know "must attack" and "may not attack X" but not "must attack X". Totem armor (Bear Umbra, Snake Umbra) was probed and deliberately left too: the engine has no "destroy" as a distinct event — a targeted destruction is a `move_card` to the graveyard — so there is nothing for the replacement to hook that would not also catch sacrifices and bounces. That is a design-level gap, not a wave |
| Small readings (nontoken sweep, attacking gate, trailing "It …") | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 211 merged: `destroy_all.nontoken` so Hour of Reckoning spares what it convoked with; `self_attacking` beside the existing `self_tapped` so an activation can be gated on the source attacking; and a fuser that keeps a trailing "It can't be blocked this turn" inside the trigger body it belongs to — left as its own sentence it became a top-level effect on a permanent card, where nothing runs it. The fuse is refused unless the sentence before it acted on the source, since in a trigger body "it" usually means the WATCHED object rather than the source. 3 flips (Hour of Reckoning, Glint-Horn Buccaneer, Kappa Cannoneer) — 1,326/2,009 (66.0%), 1,146 tests. City of Traitors deliberately left: "when you PLAY another land" is not "when another land enters", and approximating it would sacrifice the City to a fetched land |
| Becoming a copy | B/F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 212 merged: `become_copy` swaps the definition on a permanent already on the battlefield, where `enterAsCopy` only did it on arrival. `untilEot` records what to put back in `GameState.temporaryCopies`, handed back at cleanup beside the borrowed permanents — and only the FIRST copy of a turn records it, so copying twice still restores the printed card rather than the first copy. `keepAbilities` is Thespian's Stage's "except it has this ability", grafted onto a CLONED definition so the thing being copied is untouched. New target kind `artifact_creature_enchantment_or_land` for Mirage Mirror. 3 flips (Mirage Mirror, Thespian's Stage, Shifting Woodland) — 1,329/2,009 (66.2%), 1,149 tests. The Mycosynth Gardens is one away still: "with mana value X" needs the announced X to become a target filter |
| Adjectives the grammars were missing | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 213 merged, three small readings: a spell target kind for Swan Song's "enchantment, instant, or sorcery"; a two-capitalised-subtype spell discount ("Aura and Equipment spells you cast cost {1} less") which the `subtypesAny` filter already supported and only the reading lacked; and `subjectFilter.attacking` plus the matching leading adjective in `parseTriggerSubjectPhrase`, so "whenever an attacking creature dies" watches only the ones that were in combat. That last one needed the field in FOUR places — the filter type, the matcher, the serializer and createGame's explicit subjectFilter mapper, which is the recurring trap. 3 flips (Swan Song, Danitha Capashen, Kardur) — 1,332/2,009 (66.3%), 1,152 tests |
| Draw caps and board-wide restrictions | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 214 merged: `opponentsDrawCap` counted against the per-turn draw tally that already existed, checked in `wouldSkipDraw` so every draw path honours it at once rather than only the ones that remembered; and `all_restrict_until_eot`, a restriction laid on every creature filtered by a keyword it has or lacks, plus `withKeyword`/`withoutKeyword` on `EffectSelector` reading COMPUTED keywords so a granted flying counts. 2 flips (Sundering Eruption // Volcanic Fissure and one share) — 1,334/2,009 (66.4%), 1,154 tests. Narset compiles her draw cap now but is still one away on her enter-the-battlefield dig |
| Counts that size a pump, and doubling one permanent | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 215 merged: the five basic land TYPES join the shared counted-noun table — a Swamp is anything with the subtype, so an Urborg'd Island counts, which is the whole reason the wording is a type and not a name. `pt_until_eot.per` is resolved at BIND rather than at apply, so the count is taken as the spell resolves and a Swamp lost afterwards does not shrink the effect (CR 611.2c); the bound effect stays a plain number. `double_counters_on` is the single-permanent form of the team doubling, and doubles none into none. Both exhaustive records added in wave 206 did their job: adding five union members was a tsc error in the evaluator AND the serializer rather than a silent fall-through. 2 flips (Defile, Mossborn Hydra) — 1,336/2,009 (66.5%), 1,157 tests |
| Four targets at once, and a sweep that is a sacrifice | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 216 merged: Decimate's four target slots read through the shared head-noun table, each destruction pointing at its OWN index — sharing one would send all four at whatever the first slot picked. All Is Dust needed `coloredOnly` (a colourless permanent is spared, which is the whole card) and `asSacrifice` (a sacrifice is not a destruction, so indestructible is no help), plus a `permanents` sweep scope, since `nonland` would have spared a coloured land. 2 flips (Decimate, All Is Dust) — 1,338/2,009 (66.6%), 1,159 tests |
| A wider legendary, and a manifest for someone else | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 217 merged: Plaza of Heroes says legendary PERMANENTS where Mox Amber says legendary creatures and planeswalkers, so the two get separate scopes rather than sharing one that would silently under-count a legendary artifact. Reality Shift's manifest binds to `chosen_owner` — the owner of the creature the earlier clause exiled — because binding it to the caster would hand them a free 2/2. The compiler's local copy of the scope union now derives from `ManaAbility["anyColorAmong"]` instead of repeating it, which is how it drifted in the first place. 2 flips (Plaza of Heroes, Reality Shift) — 1,340/2,009 (66.7%), 1,161 tests |
| "The next spell you cast this turn …" | A/F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 218 merged: a permission with a use count rather than a prompt, the same shape `freeCastFromHand` uses — so it needed no client, bot or fuzz answer path. `GameState.nextSpellGrants` is spent at the cast site and the "can't be countered" half RIDES the stack object from there, so a second spell the same turn is not protected; the improvise half is read by `grantedCostKeyword`, which already merged permanent-granted keywords. Unspent grants expire with the turn. 2 flips (Mistrise Village, Archway of Innovation) — 1,342/2,009 (66.8%), 1,164 tests |
| A third spell union, and a restriction that lifts | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 219 merged: `artifact_creature_or_planeswalker_spell` for Strix Serenade; and `ControlledGate.atLeast`, which makes the gate COUNT rather than ask whether any one permanent satisfies it. Topiary Stomper stores its gate as `requiresControlledBelow` — the printed "unless" says when the ability STOPS, so the static is hung on the negation rather than on the condition. 2 flips (Strix Serenade, Topiary Stomper) — 1,344/2,009 (66.9%), 1,166 tests |
| "…for each thing destroyed this way" | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 220 merged, **the sixty-seven per cent crossing**: the rider goes ON the sweep, mirroring Culling Ritual's mana, because the sweep is the only thing that knows its own body count. It attaches to EVERY trailing sweep, not just the last — "destroy all artifacts and enchantments" is two sweeps, and a rider on the last alone would have counted half the board and compiled perfectly cleanly. Refused outright when the previous effect is not a sweep, rather than emitting a rider that reads nothing. 2 flips (Fumigate, Bane of Progress) — 1,346/2,009 (67.0%), 1,168 tests |
| "Up to two target …" | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 221 merged: "up to N" is N OPTIONAL SLOTS, which is a clause-level shape — the noun-phrase parser can only say "up to one", because a phrase is one requirement. The plural head is singularised and then goes through the shared target grammar rather than being parsed a second way. 1 flip (Angel of the Ruins) — 1,347/2,009 (67.0%), 1,169 tests |
| **PRIMITIVE: granted-ability statics** | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-23 | Done in two commits, both halves of one primitive: `grant_trigger` and `grant_activated` are layer-6 continuous effects, and `triggersOf` / `activatedOf` return printed-then-granted as ONE index space so no candidate, stack object or prompt needs a discriminator. Every index-resolving site rewired (batch dispatcher, begin-combat sweep, targeting and mode prompts, activation validator, activation entry point, resolver, battlefield enumeration, fuzzer, both client readers). Stack objects snapshot the grant, because an ability on the stack outlives its source (CR 113.7a) and a grant does not. 11 tests; compile rate unchanged at 1347/2009 (67.0%) by design — this is the primitive, not its readers. **Corpus finding: the value is in the ACTIVATED half, not the trigger half.** Of the granted-ability misses in the top 2000, one wants a granted trigger (Passionate Archaeologist) and eight want a granted activated ability. Kaldra Compleat turned out NOT to need this at all — its miss is a printed `watch: "attached"` trigger head |
| The quoted-grant grammar | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-23 | Wave 222 merged, **+3 to 1350/2009 (67.2%)**: one grammar for `<subject> ha(s|ve) "<quoted ability>"`, hooked into `parseGrantPredicate` AHEAD of its verb split, so the whole subject vocabulary of `parseGrantSubject` came free — "Equipped creature", "Lands you control", "Creature tokens you control", "Other creatures you control". The body dispatches on its own grammar, not on the subject: a trigger head, a mana ability, an activated ability, or a bare keyword. **A granted "{T}: Add …" must stay a MANA ability** — it must never use the stack. The hand-rolled `grantAnyMana` regex (four fixed subjects, one body shape) is deleted. Also: "As long as you control N or more &lt;type&gt;s" now rides `requiresControlled` (The World Tree). **Latent bug found by the negative case: `copyControlledGate` in createGame.ts never learned `atLeast` (wave 219), so every counted gate silently lost its count going through `createCardDefinition` and asked "do you control a land" instead of "do you control six". The copier is destructured now, so a new field is a tsc error there.** Flipped: Bootleggers' Stash, Insidious Roots, The World Tree; Paradise Mantle's wave-170 refusal is now obsolete and its test inverted |
| Protection from things that are not colors | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-23 | Wave 223 merged, **+3 to 1353/2009 (67.3%)**: `protectionFrom` went from a bare `Color[]` to a `ProtectionFrom` shape (colors / types / subtypes / multicolored / colorless / everything), which every field ORs. **The thirteen sites that asked "is this protected" each spelled out the colour intersection inline, which is exactly why nothing but a colour could be added** — they now all call one `protectedFromSource`. One `parseProtectionPhrase` serves the printed line, the keyword list and the grant predicate. Case is preserved through both splits: oracle text capitalises subtypes and lowercases card types, and nothing else tells "from Humans" from "from creatures" apart. Flipped: Yawgmoth, Stonecoil Serpent, Spirit Mantle. Moved closer: Teferi's Protection, Commander's Plate, Karmic Guide, Eldritch Immunity, The One Ring. **The `\b`-in-a-heredoc trap bit for the third time** (waves 211, 221, here) — a lookahead came out with a literal backspace and matched nothing; build the backslash with `bytes([92])` or use the Edit tool |
| Invisible-character hygiene (tooling) | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-23 | Not a wave, a guard. `engine/src/sourceHygiene.test.ts` fails the ordinary `npx vitest run` on any invisible character or mixed line ending anywhere in the repo, naming file, line, column and character name. **It found three real defects on its first run**: a literal 0x08 byte in the CLAIMS and HANDOFF notes *about* the backspace trap (the warning contained the bug), and a raw `\u0001` sentinel in `oraclePatterns.ts`, now named `PERIOD_SHIELD` and spelled as an escape. **The cause is that the Bash tool cannot write a doubled backslash** — every path through it, quoted heredoc included, collapses `\\` to `\`, so JS source that needs two gets one and reads it as an escape (a string meant to hold `\\b` becomes one holding a backspace). Verified 2026-08-23. Two earlier explanations in this file and MACHINERY.md — "a python heredoc", then "any escape-expanding writer such as `echo -e`" — were both wrong, which is most of why it recurred three times. Build the character with `String.fromCharCode(92)`, or use the Write/Edit tools. Regex LITERALS are unaffected; it is regex-in-a-string that breaks. Also cleared the last two oxlint warnings so the lint is now silent and a new warning is signal; one was a single-backslash `\d` in `COST_UNIT` that matched "Sacrifice dd Goblins" and failed "Sacrifice 3 Goblins" (latent — no card in the bulk uses the digit form). Rate unchanged at **1353/2009 (67.3%)**, 1,194 tests, lint clean, 200/200 fuzz. |
| Trigger conditions: counters on self, per-turn tallies | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-23 | Wave 224 merged, **+3 to 1356/2009 (67.5%)**: Runaway Steam-Kin, The Gaffer, Bennie Bracks. The intervening `if` is the sole blocker on 16 d1 cards but the conditions are nearly all distinct, so this took the three sharing machinery already present. **Two of the three needed no new state**: `created_token_this_turn` reads the existing `createdTokenThisTurn` list, and `self_counter_count` reads counters on the source the way `self_no_counter` does. Only `gained_life_this_turn` added a tally, and it rides the `gains_life` EVENT rather than the three separate `player.life +=` sites — one of which dispatches no event at all, so a per-site tally would have silently missed it. Also collapsed two inline copies of the counter-key normaliser into `counterKeyOf`. Moved closer: Simic Ascendancy (d2 to d1). Verified NOT condition-blocked, so left alone: Alesha (body needs "mana value less than or equal to ~'s power"), Jadar (decayed tokens), Zacama, Kazuul. |
| Damage that spends the amount the trigger carried | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-23 | Wave 225 merged, **+2 to 1358/2009 (67.6%)**: Ingenious Artillerist, Kediss. `deal_damage.amount` gained `subject_amount`; the scopes came free because `expandEachOpponent` already fanned player-targeted damage out. Retired the documented Kediss refusal with `each_other_opponent`, which expands to NOBODY without a subject rather than falling back to every opponent. Kediss also needed `subjectFilter.commanderOnly` — named to match `EffectSelector.commanderOnly` rather than inventing a second word. **Found a real pre-existing bug**: the state parser attached a trigger's `subjectFilter` only if one of TEN named fields was present, out of ~25, so a filter of only `legendary` / `attacking` / `modified` / `historic` / `colorless` lost the WHOLE filter on a round trip and then fired for every subject. Now keyed on "has any field". Brash Taunter moved d2 to d1. |
| Exhaustiveness guard for the subjectFilter copy | D | open | | | 2026-08-23 | `createGame.ts` copies `subjectFilter` field by field with no guard, so wave 225's new field was silently dropped until a test caught it — the same shape as the `copyControlledGate` bug wave 222 found. `copyControlledGate` got a destructuring exhaustiveness guard; this copy (and the parallel one in `serialize.ts`) still has none, and it has ~25 fields. Give it the same guard so the next new field is a tsc error rather than a silent drop. |
| **PRIMITIVE: a real "destroy" event** | E | open | | | 2026-08-22 | Targeted destruction is currently a `move_card` to the graveyard, so there is no event for a replacement to hook that would not also catch sacrifices and bounces. Blocks totem armor (Bear Umbra, Snake Umbra), regeneration (Asceticism, Swarmyard, Golgari Charm, Nightscape Familiar), and "can't be regenerated" riders. **Widest change proposed since tagged mana** — its own session, after granted-trigger statics has proved the pattern |
| Spree mode bodies | A | open | | | | |
| Hideaway trio | A | open | | | | Needs 3 per-turn tallies |
| Multikicker | A | open | | | | |
| Rebound | A | done | claude/2daf8b18 | comprehensive-plan | 2026-08-21 | Merged in wave 125 |
| Evolve (+ second lines) | A | done | claude/2daf8b18 | comprehensive-plan | 2026-08-21 | Wave 129 merged: 2 flips (Fathom Mage, Pollywog Prodigy), counter_added event added — 917/2,009 (45.6%) |
| Living weapon | A | done | claude/2daf8b18 | comprehensive-plan | 2026-08-21 | Wave 125 merged with rebound + tapped-unless-basic: 3 flips (Nettlecyst, Ephemerate, Quantum Misalignment), 905/2,009 (45.0%). Kaldra needs granted-trigger statics |
| Small keywords (dethrone, split second, compleated, delirium, cascade ×2) | A | open | | | | One card each |
| Station cycle | A | open | | | | Fringe cards; lowest priority |
| Modal trigger prompt + modal dies/enters cards | B | done | claude/2daf8b18 | comprehensive-plan | 2026-08-21 | Waves 126–127 merged: choose_trigger_mode machinery + 6 flips (Aether Channeler, Felidar Retreat, Retreat to Coralhelm, Atsushi, Junji, Charming Prince) — 911/2,009 (45.3%). Insidious Fungus/Cankerbloom (sac-modal ACTIVATED abilities) remain open under stream B |
| Sac-modal activated abilities + Eerie Interlude | B/D | done | claude/2daf8b18 | comprehensive-plan | 2026-08-21 | Wave 131 merged: ActivatedAbility.modes + mass delayed blink, 3 flips (Insidious Fungus, Cankerbloom, Eerie Interlude) — 921/2,009 (45.8%) |
| Confluences (choose-with-repeats) | B | open | | | | |
| Opponent-chooses (Fact or Fiction) | B | open | | | | |
| Gift mechanic | B | done | claude/2daf8b18 | comprehensive-plan | 2026-08-21 | Wave 134 merged: promise/decline mode pairs, 4 flips (Parting Gust, Into the Flood Maw, Dawn's Truce, Long River's Pull) — 938/2,009 (46.7%) |
| Academy Manufactor | C | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 197 merged as one family with the other token-creation replacements (CR 614): `extra_token` (one extra PER BATCH, not per token — Xorn on three Treasures makes four), `substitute_tokens` (Divine Visitation swaps the template), `tokens_one_of_each` (Academy Manufactor). A replacement does not apply to what it itself creates (CR 614.5) — without that guard Peregrin Took's extra Food, which matches every token, loops on the first one. Also a counted plural subtype sacrifice cost ("Sacrifice three Foods"): both `sacrificeSubtype` and `sacrificeCount` already existed, but COST_UNIT did not recognise the phrase as a cost, so it never reached the parser. 5 flips (Academy Manufactor, Divine Visitation, Stridehangar Automaton, Xorn, Peregrin Took) — 1,276/2,009 (63.5%), 1,090 tests |
| Sylvan Library | C | open | | | | Draw-step replacement |
| Waste Not | C | done | claude/2daf8b18 | comprehensive-plan | 2026-08-21 | Wave 130 merged: discards event + watchers, 2 flips (Waste Not, Bone Miser) — 918/2,009 (45.7%). Tergrid remains open |
| Mana persistence (Electro, Ashling) | C | open | | | | |
| Enters-tapped-unless-basic | C | open | | | | 2 lands |
| Sagas | D | open | | | | Urza's Saga is the #1 top-2,000 miss |
| Phasing (Clever Concealment, Eerie Interlude) | D | open | | | | |
| Extended impulse durations | D | open | | | | Shared with stream B (Atsushi) |
| Dynamic self-discounts (Colossus/Henge quartet) | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-21 | Wave 128 merged: all four discounts compile; Great Henge + Excalibur flip (914/2,009 = 45.5%). Colossus still needs a graveyard sac-2 activation, Skullspore a dies-batch-total token — both rows below |
| Phyrexian mana payment | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 168 merged: `parseAbilityCost` no longer rejects Phyrexian pips (the payment path already charged 2 life for them — only the parser was refusing), `COST_UNIT` admits counted sacrifices, `ActivatedAbility.sacrificeCount` with the extra fodder auto-taken cheapest-first (documented), `another_creature_or_artifact` scope so "two OTHER" excludes the source, and keyword counters grant their keyword (CR 122.1e) — 2 flips (Mondrak, Zopandrel) — 1,127/2,009 (56.1%), 958 tests. Drivnod still needs its exile-from-graveyard cost; Spellskite needs retargeting-to-self |
| Subtype sacrifice costs | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 169 merged: "Sacrifice a Desert/Goblin/Food" — a `permanent` scope plus a lowercase `sacrificeSubtype` (`costSacrificeSubtype` on mana abilities) rather than a union member per subtype, filtered through the shared `cardMatchesSubtype` so changelings and Humility both work. 4 flips (Scavenger Grounds, Skirk Prospector, Siege-Gang Commander, Gilded Goose) — 1,131/2,009 (56.3%), 962 tests. `parseAbilityCost` now refuses any cost text with an unmatched "Sacrifice" clause, which is what keeps Fountainport's "Sacrifice a token" from compiling to a free sacrifice; the fifth copy of the scope matcher (in `manaOptions.ts`) was also missing `treasure` and has been corrected |
| Granted quoted abilities on attachments | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 170 merged: `Equipped/Enchanted creature … has "<trigger>"` splits into the buff and the quoted trigger, the latter rewritten onto the Equipment's own `watch: "attached"` — no second address space for granted trigger indexes. The enchanted wording now shares the three attached heads; "put that many +1/+1 counters on it" reads the combat damage off the trigger; the Eldrazi Spawn reminder line compiles as a no-op because tokens.ts already gives that token the ability. 5 flips (Diamond Pick-Axe, Power Fist, The Reaver Cleaver, Awakening Zone + one share) — 1,136/2,009 (56.5%), 966 tests. Refused: quoted ACTIVATED grants (Paradise Mantle) — the rewrite would tap the Equipment. Deferred: Umbra armor (totem armor needs a destruction replacement, and destruction is a move_card here, not an event); myriad; Kaldra Compleat's damage-to-a-creature head |
| Target noun phrases | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 171 merged: `parseSimpleTargetPhrase` and `parseGraveyardTargetPhrase` replace the enumerated wordings in the blink, targeted-untap and graveyard-recursion branches (three hand-rolled regex families collapse into two grammars). Found and fixed a latent hole while wiring it: the `permanent` target branch never ran `violatesCharacteristicFilter`, so `legendaryOnly` on a permanent was inert — Minamo would have untapped anything. 10 flips (Restoration Angel, Felidar Guardian, Teleportation Circle, Unearth, Goblin Engineer, Minamo, Patriar's Seal, Kiora's Follower + 2 shares) — 1,146/2,009 (57.0%), 970 tests. Deferred: Persist (needs a nonlegendary qualifier and an enters-with-counter rider), Eldrazi Displacer (Devoid, and a tapped return) |
| Draw-step and main-phase triggers | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 172 merged: two turn steps that never dispatched `step_begins` now do — the draw step (after the turn-based draw, CR 504) and the precombat main. `draw_step` and `first_main_phase` join the trigger events, the step head reads them plus "each player's", and a `self_tapped` intervening "if" gives `triggerConditionHolds` the watcher's own id for the first time. 3 flips (Mana Vault, Hulking Raptor, Black Market) — 1,149/2,009 (57.2%), 972 tests. Deferred: Teferi's Puzzle Box (hand-to-bottom then redraw), Carpet of Flowers (both main phases plus a once-per-turn mana latch) |
| Cast-trigger grammar | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 173 merged: twelve near-identical "Whenever … casts a … spell" branches collapse into one grammar (watcher from the subject, filter from the descriptor) plus `parseSpellDescriptor`; `historic` joins the subject filter as artifact/legendary/Saga. Separately, "is put into a graveyard from the battlefield" normalizes to "dies" for the card itself (CR 700.4) and "return it to its owner's hand" reads the trigger's subject. 5 flips (Jhoira, Teshar, Niv-Mizzet, Ichor Wellspring, Rancor) — 1,154/2,009 (57.4%), sample 97%, 975 tests. Painful Quandary still needs "loses 5 life unless they discard a card" |
| X-count token creation | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 174 merged: the token clause takes a count of X (announced, zero makes none) or "where X is the number of \<Subtype\>s you control", keeps extra printed card types on the token (artifact creature, land creature), and parses two-colour printings. A literal count still emits that many effects, so no existing caller changed. 7 flips (Secure the Wastes, Grand Crescendo, Krenko Mob Boss, Myrel + 3 shares) — 1,161/2,009 (57.8%), 977 tests. Awaken the Woods still misses on its second fragment |
| Damage-modifying replacements | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 175 merged: `CardDefinition.damageReplacement` (times / plus / sourceColors / sourceMustBeCreature / opponentsOnly) plus `damageAfterReplacements` in derived.ts, called at all five places damage is applied — targeted damage to a player, to a permanent, both halves of a sweep, and both combat sites. Combat damage, commander damage, lifelink and the `combat_damage_to_player` amount all carry the modified number. 4 flips (Fiery Emancipation, Torbran, Gratuitous Violence, Twinflame Tyrant) — 1,165/2,009 (58.0%), 981 tests. Bloodletter of Aclazotz is life LOSS not damage, and Lae'zel is counters — both still open |
| Mana multipliers and cost taxes | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 176 merged: `CardDefinition.manaTapMultiplier` multiplies a tapped permanent's addition (holders multiply together, noTap abilities exempt), and `CostReduction` gains `scope` ("you" default / "opponents" / "all") plus `notDuringControllersTurn`, so a tax is a discount with a negative generic rather than new machinery — `castCostReduction` now walks every battlefield permanent, not just the caster's. 5 flips (Mana Reflection, Nyxbloom Ancient, Grand Arbiter, Defense Grid, Helm of Awakening) — 1,170/2,009 (58.2%), 984 tests |
| Sweep noun phrases | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 177 merged: `parseSweepPhrase` peels qualifiers off "all \<phrase\>" from both ends, `destroy_all` gains typesAny / tapState / withoutCounters / notEnchanted / notLegendary / toZone / minPowerAboveTarget, and "Destroy all X and all Y" compiles to two sweeps. Placed after the narrow branches so nothing already claimed changed shape. 9 flips (Fell the Mighty, Split Up, Damning Verdict, Nevinyrral's Disk, Urza's Ruinous Blast, In Garruk's Wake, Winds of Rath, Evacuation + one share) — 1,179/2,009 (58.7%), 989 tests |
| Alternative cast costs | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 178 merged: `CardDefinition.altCost` (life / exileFromHand / sacrificeCreature / a ControlledGate) with `altCastPayment` in derived.ts deciding what it would take. Taken only when the printed cost is unpayable, cards auto-picked cheapest-first — so it only ever enables an impossible cast, never replaces a better line. Paid in applyCast beside the other cost halves; castability reflects it so the spell shows with no mana at all. 5 flips (Force of Will, Misdirection, Snuff Out, Flare of Denial, Flare of Duplication) — 1,184/2,009 (58.9%), 992 tests |
| Non-mana activation costs | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 179 merged: `removeCounterCost` / `addCounterCost` / `discardCost` / `millCost` / `exileFromGraveyardCost` on ActivatedAbility, read by one `activationNonManaPayment` helper shared by the activation path and legal-action enumeration. COST_UNIT widened for all five plus counted sacrifices without "other"; the refuse-what-you-cannot-read guard extended to Remove/Discard/Mill. An activated body's leading "It" now rewrites to ~ (in a trigger body "it" is the watched object, which is why the rewrite lives in splitAbility). 11 flips (Walking Ballista, Devoted Druid, Fauna Shaman, Tortured Existence, Millikin, Dragon's Hoard, Mikaeus, Ominous Seas, Mines of Moria, Sai + one share) — 1,195/2,009 (59.5%), 996 tests |
| "For each" count table | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 180 merged: `DYNAMIC_COUNTS` + `parseDynamicCount` replace the two hand-listed count tables in the compiler, `DynamicCount` gains six kinds (creature cards in graveyard, colours among permanents, colourless creatures, creatures with a counter, Auras attached, Auras and Equipment attached), `dynamicCountOf` is exported and takes the source id for the attachment counts, and `draw`/`gain_life` gain a shared `perDynamicCount`. Every row admits singular and plural. 6 flips (Faeburrow Elder, Kor Spiritdancer, Wight of the Reliquary, Tomb of the Spirit Dragon, Venser's Journal + one share) — 1,201/2,009 (59.8%), 1,000 tests. Thran Power Suit still needs the "and has ward {2}" half; Inspiring Call the indestructible rider |
| Until-EOT effects with an X | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 181 merged: `pt_until_eot` and `team_pt_until_eot` take `"x"` (the announced X, read at bind), and the team form also `"greatest_power"`; the "+X/+X" branch in `compileUntilEotGrant` stops being a documented drop. A trailing "where X is …" printed AFTER the duration is moved back before the clause splits. `subtypes` on both team effects, filtered through `cardMatchesSubtype`. 5 flips (Tyvar's Stand, Overwhelming Stampede, Moonshaker Cavalry, Lathliss, Lord of the Accursed) — 1,206/2,009 (60.0%), 1,003 tests. Documented narrowing: "All Zombies" reaches the controller's only. Triumph of the Hordes still needs infect |
| Zone moves by filter | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 182 merged: "Put target \<type\> card from your graveyard on top of your library" reuses `parseGraveyardTargetPhrase` with a library-top destination (+ `own_graveyard_enchantment_card`), and "You may put a \<type\> card from your hand onto the battlefield" compiles to a `choose_card` over the controller's hand — the "may" IS the choice. `CardFilter` gained `equipment` and `basic_land`. 6 flips (Academy Ruins, Hall of Heliod's Generosity, Mortuary Mire, Volrath's Stronghold, Stoneforge Mystic, Terrain Generator, Monster Manual) — 1,212/2,009 (60.3%), 1,006 tests |
| Conditional mana upgrades | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 183 merged: `ManaAbility.upgrade` = a list of `ControlledGate`s (all must hold, so the Urza lands can name two different permanents) plus either a replacement pool or a new count for a colour choice. Applied in the tap path before the mana multipliers. Named permanents split on whitespace only, matching `parseTypeLine` — "Power-Plant" is one subtype, which the runtime test caught. 4 flips (Urza's Tower, Urza's Mine, Urza's Power Plant, Ilysian Caryatid) — 1,216/2,009 (60.5%), 1,009 tests |
| Counter placement grammar | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 184 merged: the enumerated "put a +1/+1 counter on \<one of four subjects\>" branch becomes `parseCounterList` plus the shared target-phrase parser, so counter lists (Gift of the Viper) and minus counters (Contagion Clasp) come free. `counter_on_each_creature` gained a real `opponentsOnly`/`opponentsOf` scope — the first attempt passed `controllerId: "each_opponent"` as an excess property that a conditional spread hid from tsc, the same silent-drop shape this file keeps recording. 2 flips (Contagion Clasp, Gift of the Viper) — 1,218/2,009 (60.6%), 1,013 tests |
| Until-EOT grant subjects and PT arithmetic | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 185 merged: `parseEotSubject` delegates to `parseSimpleTargetPhrase` instead of matching five exact wordings; that phrase gained a trailing-qualifier loop (mana value, power, possessor) and the `attacking`/`multicolored` adjectives; a P/T modifier is read as two independent signed terms, admitting `+X/+0` and `-X/-X` (new `minus_x`). Two inert filters fixed: `legalChoicesForRequirement` listed every creature for a `creature` requirement while `isChosenTargetLegal` enforced its qualifiers, and the unbound `pt_until_eot` parser rejected the `"x"` its own type had allowed since wave 181. 2 flips (Heliod Sun-Crowned, Null Elemental Blast) — 1,220/2,009 (60.7%), 1,022 tests. Deferred: Defile's "for each Swamp" scale (no scale factor on the grant, no basic-land row in the count table); Kessig Wolf Run and Grim Hireling are now blocked only by `{X}` in an ACTIVATION cost, which nothing supports — that is the next cluster |
| Control change (gain control of a permanent) | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 186 merged: `gain_control` / `gain_control_all` (optionally narrowed to one player's permanents, so "all artifacts that player controls" reads the trigger subject) / `restore_control`, plus `GameState.temporaryControl` handed back at cleanup. A stolen permanent stays in its owner's zone list, goes summoning-sick (CR 613.7) and leaves combat (CR 506.4); a second steal in one turn keeps the FIRST handback record so it goes home, not to the previous thief. The real find was a bug CLASS: every "what does this player control" site read that player's own battlefield list and filtered by controller — a subset that drops anything controlled but not owned, correct only while nothing could change control. Ascend, the attack tax, `legalActions` sacrifice fodder, populate and the Army lookup now share one `permanentsControlledBy`. 3 flips (Archmage's Charm, Hellkite Tyrant, Homeward Path) — 1,223/2,009 (60.9%), 1,029 tests. Deferred: Insurrection ("gain control of them" / "They gain haste" need a back-reference to the set the previous clause touched); Treasure Nabber (no "taps a permanent for mana" trigger for opponents' permanents) |
| Token descriptor grammar | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 187 merged: `parseTokenDescriptor` replaces the branch-per-wording token shapes — count (including past ten and "a number of"), "tapped"/"tapped and attacking", P/T, colours, supertypes, subtypes and card types (told apart by CAPITALISATION, since oracle text capitalises subtypes and lowercases card types), "named X", keyword tail, and two tokens in one clause split at the "and" followed by a count word. Trailing count tails ("for each creature you control", "for each +1/+1 counter on ~", "equal to its power") become one scaled effect. `create_token.colors` added — a token has no mana cost to derive colours from, so a "blue and red Elemental" was colourless to every colour filter. Two finds: a clause with no targets of its own is no longer renumbered on merge (its chosen refs point at an EARLIER clause's targets — shifting them bound the effect to nobody while the card still reported a clean compile), and a token name quoting its own card ("Kobolds of ~") gets the placeholder undone at bind time. 8 flips (Army of the Damned, Kher Keep, Wurmcoil Engine, Resculpt, Nuka-Cola Vending Machine, The Battle of Bywater, Hangarback Walker + one share) — 1,231/2,009 (61.3%), 1,037 tests. Known laxity left in place: the older shapes still sit below the grammar for counts read off the board and quoted granted abilities, and their subtype match accepts any unknown word as a subtype |
| Trigger head subject grammar | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 188 merged: `SUBJECT_HEAD_VERBS` table (attacks, becomes tapped/untapped, deals combat damage to a player, deals damage to a player, leaves the battlefield) plus `parseTriggerSubjectPhrase` extracted so every head shape shares one subject parser — possessor (`you control` / `an opponent controls`), attached subjects (equipped/enchanted creature), leading adjectives (nontoken/token/legendary) and trailing qualifiers (`with`/`without <keyword>`, `with mana value N or greater`). Order is load-bearing: "a creature you control WITH FLYING" puts the keyword outside the possessor, so it strips first. New `subjectFilter.withKeyword/withoutKeyword/legendary/minManaValue`. Sacrifice heads now name the sacrificer separately from the permanent, which exposed an inert branch: `player_sacrifices` dispatch returned true for ANY sacrifice by ANY player, ignoring watch/excludeSelf/subjectFilter — invisible while only Mayhem Devil's unrestricted head used it. 5 flips (Dragon Tempest, Mazirek, Kudo + 2 shares) — 1,236/2,009 (61.5%), 1,043 tests. 27 of the 60 one-away trigger heads now parse; the rest need events that do not exist (taps for mana, plays a land, is exiled from the battlefield, becomes blocked) |
| Counter subjects and attaching | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 189 merged: counter placement accepts `equipped`/`enchanted creature` (the `host` selector) and `each <Subtype> you control` (`counter_on_each_creature.subtype` + controlledOnly); attach is now a clause reading its subject and host separately (Hero's Blade, Hammer of Nazahn), with the printed "may" auto-taken. Latent fix: the attach effect passed `toId` through verbatim when it was a string, so a NAMED selector ("self"/"subject_card"/"host") was treated as a literal card id and the attachment went nowhere — it binds through `bindCardId` now. 5 flips (Cordial Vampire, Hammer of Nazahn, Hero's Blade, Tarrian's Soulcleaver + one share) — 1,241/2,009 (61.8%), 1,048 tests. **Cluster-picking note: the damage clauses were the obvious next collapse (EIGHTEEN branches, more than the token family that paid eight) and were not worth doing — the miss list held only two damage-recipient gaps. Counting branches is half the test; the other half is whether the miss list still wants what they cover.** |
| Trigger subject nouns and spell colours | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 190 merged: the head's adjective loop was eating "token" in "a token you control" and leaving nothing for the head noun — adjectives now require a following word, and a bare "token" head becomes a token-filtered permanent. Added `subjectFilter.modified` (CR 701.48: an Aura/Equipment attached OR any counter), colour spell descriptors ("a red spell"), and an opponents-only narrowing on `casts_second_spell` so Monologue Tax does not pay its own controller. 3 flips (Nadier's Nightblade, Monologue Tax, Kodama of the West Tree) — 1,244/2,009 (61.9%), 1,053 tests |
| Ability words and conditional riders | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 191 merged: ability words (CR 207.2c) stripped in `normalizeOracleText` from an EXPLICIT LIST — boast/channel/imprint/strive also print before an em dash and carry real rules, so a shape-based strip would widen those abilities. Behind them was one clause shape: `<effect> instead if <cond>`, `If <cond>, [instead] <effect>`, and the additive `<effect> … if <cond>` — all compiling to a new `if_condition` effect whose branch is picked at BIND (a spell's resolution). Condition vocabulary shared with trigger intervening-ifs; the serializer's inline copy extracted to `parseTriggerCondition` so the two cannot drift. New conditions: graveyard_cards_at_least, graveyard_card_types_at_least (counts TYPES not cards), creature_died_this_turn. 6 flips (Cabal Ritual, Tragic Slip, Stubborn Denial, Demonic Counsel, Temur Battle Rage, Fanatic of Rhonas's ability) — 1,250/2,009 (62.2%), 1,060 tests. Open: "exile that creature" / "return that card" back-references (Dispatch, Stitch Together) — same gap as Insurrection and Origin of Metalbending |
| Back-references and narrowed flash | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 192 merged: `compileBackReferenceClause` reads "exile that creature" / "return that card to the battlefield" as the target an EARLIER clause chose — gated on the card having declared one, so an orphan back-reference is a clean miss rather than an effect bound to nobody; works through an "instead" rider (Stitch Together). `CardDefinition.grantsFlashFor` narrows a flash grant to named spells, with `hasFlashGrant` taking the spell being cast. **The narrowed grant nearly shipped broken: the compile branch made the cards report FULLY COMPILED and the rate rise, while oracle.ts never copied the field onto the definition — the compile-rate metric cannot see that class of bug by construction, only the test caught it. Fourth mapper layer, again.** 4 flips (Dispatch, Stitch Together, Sigarda's Aid, Shimmer Myr) — 1,254/2,009 (62.4%), 1,065 tests |
| Shared condition vocabulary | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 193 merged: the trigger intervening-if chain (~120 lines, one branch per wording) collapsed into `parseEffectCondition`, now shared by trigger heads, `Activate only if …` (new `ActivatedAbility.requiresCondition`, checked at BOTH activation and legal-action enumeration) and ability-word riders. New conditions: opponent_controls_count (ANY single opponent, not the table total), graveyard_creature_cards_at_least, attacked_this_turn, drew_cards_this_turn, controls_colored_permanent, and `controls_subtype_count.excludeSelf` ("five OTHER Mountains" — counting itself would meet the bar one early). Plus "you may have ~ deal N damage" rewriting to the ordinary damage clause. 3 flips (Valakut, Alesha, + one share) — 1,257/2,009 (62.6%), 1,070 tests. Note: Valakut's one-away line was TRUNCATED and hid the real blocker — read the printed card, not the report |
| Multi-sentence modal bullets | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 195 merged: a modal bullet may now be several sentences — each compiles alone and they join, with later chosen indexes renumbered onto the tail of the BULLET's own target list (bounded to the bullet, since a mode's targets are chosen for that mode alone). A back-reference inside a bullet ("It gains indestructible") rebinds to what the bullet's earlier sentence targeted, since the grant parser reads "It" as the TRIGGER subject. Plus `tap_all`, a loot and a token aimed at a chosen player, and the token-creature / planeswalker edict filters. **Two near-misses caught by tests: `tap_all` was missing from `expandEachOpponent` (would have THROWN at bind), and the edict's planeswalker check matched a bare "planeswalker" as well as "creature or planeswalker", widening a planeswalker-only edict to take creatures.** Printed order is "a creature TOKEN" — noun then noun, not an adjective in front. 5 flips (Origin of Metalbending, Cryptic Command, Prismari Command, Sheoldred's Edict + one share) — 1,268/2,009 (63.1%), 1,080 tests |
| X in activation costs | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 196 merged: `ActivatedAbility.xCost` (pip count — `{X}{X}` charges twice), `activate_ability.xValue`, threaded through `putActivatedAbilityOnStack` → `StackObject.xValue` → the resolution bind context, so a body reading X gets the number the cost charged. `amass` amount accepts "x" (zero amasses nothing rather than the default one). Guards both ways: an ability with {X} refuses to activate unannounced, one without refuses an announcement. The FUZZER announces zero — enumeration only promises the BASE cost is affordable, so a random X could exceed the mana that made the action legal. **Fifth mapper layer found: the compiler rebuilds the ability field-by-field at the push site, so `xCost` parsed, typechecked and never reached the definition.** 3 flips (Kessig Wolf Run, Treasure Vault, Barad-dûr) — 1,271/2,009 (63.3%), 1,084 tests. Still open behind this: Mycosynth Gardens (copy with mana value X), Mirror Entity (base P/T X/X), Steel Hellkite, Grim Hireling ("Sacrifice X Treasures" — a variable COST count, not a variable mana cost) |
| Costs that could already do it | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 198 merged: five gaps that were all REFUSED WORDINGS rather than missing features. `all_pt_until_eot` already took "-x" and the grant refused to pass one; "each creature" was not an alias for "all creatures" (The Meathook Massacre needed both). `CostReduction.condition` from the shared vocabulary (Bolt Bend). `costTapCreatureLegendary` — and Relic of Legends has NO {T} of its own, the creature tap is the whole cost, which the mana-ability branch had required. "Sacrifice a land" as an effect = the edict's choose-a-permanent shape. 10 flips — 1,281/2,009 (63.8%), 1,095 tests. **Lesson: check whether the capability exists and the READING is missing before building anything; a refused wording is far cheaper to diagnose than a feature is to add.** |
| Excluded types and Class levels | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 199 merged: `TargetRequirement.excludedTypes` ("noncreature artifact or noncreature enchantment" — the adjective repeats on each half and is lifted off both); the destroy/exile clause now reads `parseSimpleTargetPhrase` instead of a 12-noun list, with those three head nouns moved into the shared table; `class_level` trigger event; `lose_life.perDynamicCount`. **THE FIND: the whole permanent-target family (artifact / enchantment / artifact_or_enchantment / nonland_permanent / planeswalker / commander / …) recursed into the permanent check with a BARE {kind:"permanent"} requirement, so it never saw its own qualifiers — excludedTypes, legendaryOnly, multicolored, both power bounds, nontoken and both subtype filters were INERT across every one of those kinds. Same shape as the wave-171 fix, an order of magnitude wider.** 3 flips (Haywire Mite, Wizard Class, Castle Locthwain) — 1,284/2,009 (63.9%), 1,099 tests |
| Subject-player bodies and token fodder | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 200 merged: "that player mills/draws/discards N" reads the trigger's subject player, and mill takes "that many" as `subject_amount` (only mill — a draw of "that many" has no printed number here and would be a guess, so it is refused). Counted sacrifice as an EFFECT = one pick per permanent, placed in `compileSimpleClause` because trigger bodies are where it appears. `token` added as a fodder scope on the shared `sacrificeScopeMatches`, so every caller got it at once — it had been REFUSED since wave 169 so the sacrifice would not compile to nothing, and that test now guards an unnameable scope instead. 3 flips (Mindcrank, Lotus Field, Fountainport) — 1,287/2,009 (64.1%), 1,103 tests |
| Target-referent conditions | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 201 merged: `chosen_has_subtype` condition + a counter back-reference, so "If it's a Spirit, put a +1/+1 counter on it" reads both halves against the earlier clause's target. The referent is the trigger subject in a trigger body and the first chosen target on a spell; with NEITHER the condition fails rather than passing by default. Plus `double_counters_on_team` — a one-shot doubling of what is on the board, where doubling nothing stays nothing. 2 flips (Essence Flux, Bristly Bill) — 1,289/2,009 (64.2%), 1,106 tests |
| Enter counters and life-loss triggers | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 202 merged: `CardDefinition.entersWithCounters` (a FIXED count, applied at the arrival site beside planeswalker loyalty — CR 121.6, so a 0/0 entering with four is a 4/4 and never meets the SBA sweep at 0); `you_lose_life` trigger event beside the opponent-scoped one; `draw.count "subject_amount"`; and the "you may have X lose/gain/draw N" conjugating form of the auto-taken may. 5 flips (Kalonian Hydra, Suture Priest, Vilis, + 2 shares) — 1,294/2,009 (64.4%), 1,110 tests |
| Sweep type exclusions and mana gates | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 203 merged: `destroy_all.exceptTypes` ("all nonartifact creatures"); `ManaAbility.requiresCondition` so "Activate only if …" rides a MANA ability too (a separate list from the activated ones — the gate had been attaching only to the latter); active-voice counter-added head. **"nonland" is deliberately excluded from the non-type strip in BOTH the sweep phrase and the target phrase, because "nonland permanent" is already a head noun in each — the first attempt claimed it in both places and rewrote working shapes.** 3 flips (Organic Extinction, Shrine of the Forsaken Gods, Exemplar of Light) — 1,297/2,009 (64.6%), 1,113 tests |
| Table-wide grants | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 204 merged: `extraLandDropsForAll` (counted from the WHOLE battlefield, not the holder's side — the only difference from Exploration); `opponentNonbasicLandsEnterTapped` (Thalia, sparing basics); the auto-taken "you may mill N cards"; and the two-type "Sacrifice another creature or artifact" cost, whose scope already existed and only lacked a reading in COST_UNIT and SACRIFICE_TYPE_COST. 5 flips (Rites of Flourishing, Thalia, World Shaper, Umbral Collar Zealot + one share) — 1,302/2,009 (64.8%), 1,116 tests |
| Sixty-five per cent | E | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 205 merged — **THE 65% CROSSING (1,306/2,009)**. Self-return names the hand as well as the battlefield; `Exile ~` is a cost unit and `parseAbilityCost` reads it; modular = `entersWithCounters` + a dies trigger with `move_all_counters`, both halves pre-existing; "double the power of target creature" aliased. **Bug found via a STALE ASSERTION rather than a card: a counted sacrifice carried the PLURAL scope ("artifacts") — only "creatures" had been folded — so it typechecked through an `as` cast and would have matched no fodder at the table.** 4 flips — 1,306/2,009 (65.0%), 1,121 tests |
| Bolas's Citadel | E | open | | | | |
| Activation gates (Verge cycle, legendary/power/attack conditions) | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-21 | Wave 145 merged: `ControlledGate` (subtypesAny/legendary/minPower) + attacker tally, 13 flips (10 Verge lands, Rivendell, Bonders' Enclave, Minas Tirith) — 1,020/2,009 (50.8%), 887 tests. Fixed a latent divergence: the gate matcher was hand-copied into four modules and the mana-ability copy passed `subtypesAny` for any permanent |
| Static grant lines (anthems, keyword grants, aura pumps) | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-21 | Wave 146 merged: subject/predicate grammar for static grants, `opponents` scope + legendary/nonLegendary/commanderOnly/withCounter selectors, `enchantments_you_control` count — 15 flips (Intangible Virtue, Rising of the Day, Elesh Norn, Bastion Protector, Akroma's Memorial, True Conviction, Elvish Archdruid, Avacyn, Sterling Grove, Chronicle of Victory, Herald of Secret Streams, Ethereal Armor, Blackblade Reforged + shares) — 1,035/2,009 (51.5%), 890 tests. Deferred: granted ward (Flowering of the White Tree), protection from a card type (Spirit Mantle), granted activated abilities (Bootleggers' Stash), player-inclusive subjects (Shalai), annihilator (Eldrazi Conscription), wither (Massacre Girl) |
| Until-end-of-turn grants (team and targeted pumps) | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-21 | Wave 147 merged: `compileUntilEotGrant` subject/predicate grammar, 11 flips (Overrun, Gaea's Gift, Slayers' Stronghold, Bridgeworks Battle, Enduring Courage, Tamiyo's Safekeeping, Sylvan Safekeeper, Sokenzan, Overprotect, End-Raze Forerunners, Loran's Escape) — 1,046/2,009 (52.1%), 893 tests. Deferred: "+X/+X" needs the announced X in until-EOT effects (Tyvar's Stand, Kessig Wolf Run); infect (Triumph of the Hordes); "you may have <target> gain …" wrapper (Duelist's Heritage); an all-scope keyword grant (Lord of the Accursed) |
| Attached grants + granted ward | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-21 | Wave 148 merged: three narrow attached branches deleted in favour of the wave-146 grammar, `grant_ward` (layer 6) + computed ward read by the ward prompt, "loses <keyword>", singular "Each …" subjects — 6 flips (Sword of Vengeance, Lavaspur Boots, Winged Boots, Flowering of the White Tree, Colossus Hammer, Duskshell Crawler) — 1,052/2,009 (52.4%), 896 tests. Deferred: protection from a card type (Spirit Mantle), granted triggered/activated abilities in quotes (The Reaver Cleaver, Paradise Mantle, Diamond Pick-Axe), nonbasic landwalk, myriad, annihilator, "As long as …" conditional statics (Champion's Helm, Serra Ascendant, The World Tree) |
| Trigger subjects + drain bodies | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-21 | Wave 149 merged: `parseGenericSubjectHead` for enters/dies noun phrases + the drain body over five player subjects — 7 flips (Kambal, Sheoldred, Vengeful Bloodwitch, Marwyn, Starfield Mystic, Cruel Celebrant, Altar of the Brood) — 1,059/2,009 (52.7%), 899 tests. Deferred: token leaves-battlefield watches (the event only fires for permanents with counters — Nadier's Nightblade), "you sacrifice an artifact" (existing head is token-only — Crime Novelist), exile-from-battlefield watches (Soulherder), general "A and B" bodies (Undead Augur, Midnight Reaper) |
| Compound clause bodies ("A and B") | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-21 | Wave 150 merged: `compileCompoundClause` (self-validating top-level split, target-index renumbering, recursive tail) + "it" as the watched object + self-damage to you/each player — 8 flips (Undead Augur, Midnight Reaper, Sunscorch Regent, Surrak and Goreclaw and pattern-shares) — 1,067/2,009 (53.1%), 902 tests. Deferred: "enters tapped" as a trigger-head qualifier (Amulet of Vigor) |
| Step-trigger timing + intervening-ifs + win conditions | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-21 | Wave 151 merged: `eachPlayersStep`, four new intervening-if conditions, `win_game` effect, generic ability-word stripping, `parseCount`-driven counts, control-qualified creature targets — 7 flips (Felidar Sovereign, Revel in Riches, Triskaidekaphile, Ophiomancer, Knuckles the Echidna, Ravenous Chupacabra and a share) — 1,074/2,009 (53.5%), 905 tests. Deferred: "each opponent's step" (would wrongly fire on the controller's turn), three-type destroy targets (Acidic Slime, Fracture), "gained N life this turn" condition (The Gaffer) |
| Conditional statics ("As long as …") + graveyard-card targets | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-21 | Wave 152 merged: `requiresLife` gate, object-conditions as selector refinements, `own_graveyard_land_card`, `artifact_enchantment_or_land`, `artifact_enchantment_or_planeswalker` — 5 flips (Serra Ascendant, Champion's Helm, Titania, Acidic Slime, Fracture) — 1,079/2,009 (53.7%), 908 tests. Fixed a latent bug: the `attached` selector scope returned early, so every other clause on an attached selector was inert. Deferred: The World Tree (granted mana ability behind a land-count gate), Cloud (trigger doubling behind an equipped gate) |
| Spend-this-mana-only restrictions | A | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 166 merged: `PlayerState.restrictedMana` — a second pool tagged with a `ManaRestriction`, produced by `ManaAbility.spendOnly`, emptied with the main pool, spent FIRST when legal (it cannot be saved, so spending it never costs options). Payment takes a `ManaPurpose` describing the spell or ability; omitting it means nothing restricted qualifies, which is the safe default for taxes. 6 flips (Cavern of Souls, Delighted Halfling, Unclaimed Territory, Secluded Courtyard, Castle Garenbrig, Eldrazi Temple) — 1,125/2,009 (56.0%), 954 tests. Documented drop: "and that spell can't be countered" is not carried, since that needs the spend tracked onto the spell |
| Modal bullets (subtype sweeps, all-creature pumps) | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 153 merged: `destroy_all.onlySubtype` + printed `exceptSubtype` on the UNBOUND effect (the compiler was emitting a field the binder silently dropped), plural "one or more … enter/die" batch heads, all-scope until-EOT pumps — 1 flip (Crux of Fate) — 1,080/2,009 (53.8%), 912 tests. Thin on flips: most remaining modal cards fail on a DIFFERENT bullet each (Prismari Command, Archmage's Charm, Golgari Charm, Sublime Epiphany), so modals are a per-card grind, not a family |
| Landscape cycle + mass graveyard exile | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 154 merged: search descriptors keep a leading article and supertype across an any-of list (the whole five-land Landscape cycle from one fix), and `exile_graveyard` joins `expandEachOpponent` so "each opponent's graveyard" is one clause per player — 6 flips — 1,086/2,009 (54.1%), 914 tests |
| Stack and sweep odds (mass land return, spell bounce, opponent sweeps) | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 155 merged: `return_all_lands` effect, `instant_spell` target kind, `destroy_all.opponentsOnly`, "look at target player's hand" reusing `reveal_zone` — 6 flips (Splendid Reclamation, Aftermath Analyst, Lumra, Dispel, Ruinous Ultimatum, Gitaxian Probe) — 1,092/2,009 (54.4%), 917 tests. The 60-card sample reached 95%, so the CI floor rose 85 → 90. Deferred: goad-all (no goad effect exists), spell bounce (Reprieve) |
| One-turn flash grants + spell bounce | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 156 merged: `GameState.flashThisTurn` (cleared at cleanup, read by `hasFlashGrant` alongside the permanent `grantsFlash`) + `grant_flash_this_turn` effect + spell-only bounce — 4 flips (Borne Upon a Wind, Emergence Zone, Alchemist's Refuge, Reprieve) — 1,096/2,009 (54.6%), 920 tests |
| Free casting from hand | A/F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 157 merged: `GameState.freeCastFromHand` as a PERMISSION with a use count (the `exilePlayable` model) rather than a new prompt — so the existing cast action serves it and no client/bot/fuzz answer path was needed. Grants match narrowest-cap-first, are spent by one cast, and expire at cleanup. 2 flips (Rishkar's Expertise, Electrodominance) — 1,098/2,009 (54.7%), 925 tests. The same permission now unblocks Omniscience (uncapped, permanent), Bolas's Citadel (top-of-library + life), As Foretold (once per turn, growing cap) and One with the Multiverse if someone wires their triggers to it |
| Static free-cast permissions (Omniscience, As Foretold) | A/F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 158 merged: `CardDefinition.castFreeFromHand` (optional counter-derived cap, optional once-per-turn) + `GameState.freeCastUsedThisTurn` reset at untap — 2 flips (Omniscience, As Foretold) — 1,100/2,009 (54.8%), 928 tests. Documented simplification: the once-per-turn latch is per PLAYER, so a second As Foretold would not grant a second cast. Bolas's Citadel still open — same permission but from the library top, paying life |
| Draw variants (X draws, shared draws, symmetric loots) | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 159 merged: `draw.count "x"` reading the announced X, plus targeted X draws, "draw X then discard", "you and target opponent each draw", and "each player draws then discards" — 4 flips (Blue Sun's Zenith, Pull from Tomorrow, Secret Rendezvous, Geier Reach Sanitarium) — 1,104/2,009 (55.0%), 931 tests |
| Enter-choices and wider clone scopes | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 160 merged: `chooseColorExcludes` threaded to the prompt and honoured by the resolver AND all three answerers (client, bot, fuzz), plus `any_land`/`any_equipment`/`any_artifact_or_enchantment` clone scopes and `enterAsCopy.entersTapped` — 9 flips (5 Thriving lands, Valgavoth's Lair, Vesuva, Mirrormade, Masterwork of Ingenuity) — 1,113/2,009 (55.4%), 935 tests |
| Either-or additional costs | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 161 merged: `AdditionalCastCost.alternatives`, branch chosen from the cast action's own fields (no prompt), `legalActions` satisfied by ANY payable branch, serializer refactored to one recursive parser — 2 flips (Demand Answers, Bitter Triumph). Redirect Lightning stays a miss on purpose: its "or pay {2}" branch is mana, which this cost shape does not charge, and half a cost is worse than none — 1,115/2,009 (55.5%), 938 tests |
| Narrowed mana echoes | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 162 merged: `landTapEcho` generalised from a boolean to a rule (subtype gate, any-permanent, fixed colour, produced-colour gate) so Mirari's Wake, Crypt Ghast and Forsaken Monument are one mechanism — 2 flips — 1,117/2,009 (55.6%), 941 tests. Forsaken Monument's colourless cost reduction is still a separate miss |
| Damage amount on combat-damage triggers | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 163 merged: `combat_damage_to_player` carries its amount, which reaches trigger bodies as `subjectAmount` — "create that many Treasure tokens" (Old Gnawbone). 1 flip — 1,118/2,009 (55.6%), 943 tests. Kediss deliberately NOT compiled: "each other opponent" would need a selector excluding the trigger subject, and `each_opponent` would hit the player just damaged |
| Optional target slots ("up to one target") | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 164 merged: the destroy/exile target pattern reads "up to one target …" as `optional: true`. 0 flips on its own — Loran of the Third Path turned out to be blocked by something else entirely (its oracle text says "When **Loran** enters", and short-legend-name normalisation only strips the pre-comma part of a name, so a legend with no comma is never shortened). That normalisation gap is the next thing to fix here and probably covers several cards — 1,118/2,009 (55.6%), 945 tests |
| Comma-less legend short names | F | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 165 merged: `normalizeOracleText` shortens "X of the Y" / "X the Y" names to their leading word, not only the pre-comma part — Loran of the Third Path. Deliberately narrow: a plain multi-word name (Lightning Greaves) has no short form and must not be shortened. 1 flip — 1,119/2,009 (55.7%), 948 tests |
| Spend-only restriction grammar | A | done | claude/2daf8b18 | comprehensive-plan | 2026-08-22 | Wave 167 merged: `parseSpendRestriction` becomes a small grammar (article + adjectives + head noun + optional chosen-type tag + optional abilities half) rather than a fixed list. 0 flips now, but it takes Haven of the Spirit Dragon, Herd Heirloom, Giada, Great Hall of the Citadel and Power Depot from two-away to one-away. Refuses "monocolored", which the engine cannot express |
| One-away grinding (rotating buckets) | F | open | | | 2026-08-21 | Wave 144 bucket done — THE 50% CROSSING (10 flips: Eladamri's Call, Tocasia's Welcome, Ayara, Curse of the Swine, Springbloom Druid, Reassembling Skeleton, Kaya's Ghostform + 3 shares — 1007/2,009 = 50.1%). Deferred: Fiery Emancipation (damage tripling, six apply sites), High Tide (until-EOT tap replacement). Wave 143 bucket done (7 flips: Sculpting Steel, Narset's Reversal, Imp's Mischief, Goreclaw, Reflections of Littjara, Archaeomancer's Map + one share — 997/2,009 = 49.6%). Deferred: Shared Animosity (per-attacker shared-type count), Selvala (comparative draw + combo mana), Bloodchief Ascension (quest gates), Mindbreak Trap (conditional {0} + variable spell exile). Wave 142 bucket done (7 flips: Mental Misstep, Warstorm Surge, Imperial Recruiter, Kenrith's Transformation, Drannith Magistrate + Aura/Equipment-normalization shares — 990/2,009 = 49.3%). Deferred: Goreclaw (two filter additions), Spelunking (Cave rider + lands-enter-untapped), Curse of Opulence (enchant player). Wave 141 bucket done (14 flips: Greater Good, Howling Mine, Open the Armory, Idyllic Tutor, Dramatic Reversal, Reckless Fireweaver, Return to Nature, Tireless Tracker + 6 pattern-shares — 983/2,009 = 48.9%). Wave 140 bucket done (6 flips: Cloud Key, Banner of Kinship, Forgotten Ancient, The Ozolith, Puresteel Paladin + one pattern-share — 969/2,009 = 48.2%). Wave 139 bucket done (5 flips: Living Death, Tribute to the World Tree, Necropotence, Bolt Bend, Redirect Lightning — 963/2,009 = 47.9%). Probed but deferred: Dig Through Time (delve), Wishclaw Talisman (control handoff), Trouble in Pairs (extra-turn replacement), Underworld Breach (escape), Archdruid's Charm (conditional-destination tutor), Return the Favor (copy+retarget). Wave 138 bucket done (5 flips: Sanctum Weaver, Altar of Dementia, Victimize, Culling Ritual, Plaguecrafter — 959/2,009 = 47.7%). Probed but deferred: Banner of Kinship (per-counter team pump), Cloud Key (chosen card type), Torment of Hailfire (X-repeat). Wave 137 bucket done (5 flips: Dryad of the Ilysian Grove, Mystic Forge, Realmwalker, Maskwood Nexus, Kutzil — 954/2,009 = 47.5%). Probed but deferred: Puresteel Paladin (gated equip {0}), The Ozolith (counter transfer), Braids (opponent sac-or-suffer chain), Dauthi Voidwalker (graveyard replacement). Wave 136 bucket done (6 flips: Wheel of Fortune, All That Glitters, Mentor of the Meek, Second Harvest, Blasphemous Edict + one pattern-share — 949/2,009 = 47.2%). Probed but deferred: Cloud Key (chosen card type), Banner of Kinship (per-counter team pump), Torment of Hailfire (X-repeat opponent choices), Culling Ritual (mana per destroyed). Wave 135 bucket done (5 flips: Stitcher's Supplier, Lotus Cobra, Noxious Revival, Gamble, Ghostly Flicker — 943/2,009 = 46.9%). Probed but deferred: Victimize (resolution-cost sac), Plaguecrafter (each-player sac choice), Second Harvest (per-token copies), Forgotten Ancient (counter redistribution) |

## Done

| Cluster | Owner | Merged | Notes |
| --- | --- | --- | --- |
| Waves 1–122 (pre-parallelization) | claude + Liberty | through 767b206 | 15.6% → 44.3% (889/2,009), 833 tests, checkpoints 1–68 |
